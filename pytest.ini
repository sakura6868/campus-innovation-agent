[pytest]
# 冻结快照目录 backups/ 内含同名 tests 包，会污染收集导致 import file mismatch，
# 且我们从不对其跑测试；norecursedirs 排除后可消除 19 个 collection error。
norecursedirs = backups .pytest_cache .git .workbuddy node_modules __pycache__ .playwright-cli
testpaths = tests
addopts = -p no:cacheprovider
