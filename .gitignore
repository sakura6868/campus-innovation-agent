# ── Python ──
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# ── 运行时产物 / 数据 (非源码) ──
*.log
*.db
*.sqlite3
*.zip
*.pdf
output/
demo/
tmp/
backups/

# 仅保留种子数据 data/ground_truth/，排除运行时生成目录
data/raw/
data/uploads/
data/parsed/
data/chunks/

# ── 环境 / 密钥 ──
.env
.env.*
!.env.example

# ── 内部记忆 (不应入库) ──
.workbuddy/

# ── Docker ──
.dockerignore 已覆盖，此处无需重复

# ── 运行时/测试产物 ──
.playwright-cli
src/data/chunks
**/.playwright-cli/**
**/src/data/chunks/**
