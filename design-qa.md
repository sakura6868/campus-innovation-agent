# 深色路演证据板 · Design QA

## 比较目标

- **Source visual truth:** [`design-concepts/concept-e-roadshow-evidence-board.png`](design-concepts/concept-e-roadshow-evidence-board.png)
- **Implementation:** 本地 `http://127.0.0.1:8000/#/home` 及同一 SPA 的关键路由
- **Viewport:** 桌面验收 `1487 × 1058 CSS px`，`deviceScaleFactor = 1`；移动适配额外验收 `390 × 844 CSS px`
- **像素归一化:** 源图与桌面截图均为 `1487 × 1058`，无需缩放；移动截图单独按 `390 × 844` 观察，不与桌面源图作像素级比较。
- **验收状态:** 管理员本地调试登录后，默认画像为“测试·全栈开发”；首页、赛事大厅、画像、匹配建议、行动路线、通知中心、我的项目、智能顾问和数据维护均由真实本地 API/SPA 路由渲染。

## 全视图比较证据

源图与首页实现已在同一次视觉比较输入中打开。实现保留了源图的核心语法：近黑舞台底色、暖金主行动、珊瑚色风险、紫色辅助状态、衬线大标题、横向导航，以及“痛点证据 → 解决方案 → 已有进展”的证据叙事。首页实现截图：[`design-qa-home.png`](design-qa-home.png)。

实现没有机械复制源图中的静态数字，而是把同一信息层级绑定到当前画像和项目数据：舞台主 CTA 进入赛事详情/智能顾问，三张证据卡展示动态匹配结果，底部统计卡连接项目、执行清单和机会雷达。这是为了让视觉方向在真实产品中可操作，而不是一张不可用的海报。

## 聚焦区域比较证据

- **舞台与证据卡：** 首页英雄区使用真实生成资产 [`frontend/assets/roadshow-stage-background.png`](frontend/assets/roadshow-stage-background.png)，并在其上放置可编辑 HTML 文案；截图中人物、聚光灯和舞台光晕与源图保持同一艺术方向，文字仍可访问、可响应式换行。
- **数据密度与操作：** [`design-qa-hall.png`](design-qa-hall.png) 验证赛事检索卡片的深色表面、金色截止节点和搜索结果层级；输入“蓝桥杯”返回 `2 / 146 场赛事`。
- **执行时间线：** [`design-qa-timeline.png`](design-qa-timeline.png) 验证点位、图例和标签间距；页面存在 13 个 `.timeline-point`，没有文字遮挡或越界。
- **智能顾问与数据维护：** [`design-qa-agent.png`](design-qa-agent.png) 和 [`design-qa-admin.png`](design-qa-admin.png) 验证最后一批旧浅色覆盖规则已被隔离，输入区、上传区、状态卡和 CTA 均进入同一深色舞台。
- **画像选择：** [`design-qa-profile.png`](design-qa-profile.png) 验证选中画像采用暖金边框/底色，未选中卡片保持暗色对比，避免出现白色孤岛。
- **移动端：** [`design-qa-mobile-home.png`](design-qa-mobile-home.png) 验证 390×844 下英雄区改为单列、证据卡堆叠、CTA 保持可点击；顶部导航为两行四列入口网格，所有一级入口完整可见且不遮挡主内容。

## 必检视觉表面

### 字体与排版

展示标题采用系统宋体栈（`Songti SC / STSong`），正文使用现有系统无衬线栈，小型路由与标签使用等宽字母间距，形成“舞台标题 / 证据正文 / 工程状态”三级层级。英雄标题、证据卡标题和时间线标题均检查了换行与行高；长赛事名称仍在卡片内自然换行，不覆盖按钮。

### 间距与布局节奏

桌面内容采用宽容器、英雄区与证据卡的轻微叠放、三列证据板和四列统计板；项目时间线采用固定标签列与独立轨道，13 个点位均落在轨道附近。移动端通过单列堆叠和横向导航滚动降低密度，未发现控件互相覆盖。

### 色彩与视觉 token

核心 token 为 `#0b0b11` 背景、`#15131b` 卡面、`#f5c36b` 行动金、`#ff8170` 风险珊瑚、`#b49af0` 辅助紫、`#86c99f` 完成绿；浅色旧规则已在 agent/admin/profile 的末端隔离层中覆盖。表单、上传区、图表和状态徽章均使用同一套边框与对比度。

### 图像质量与资产

英雄舞台使用 PNG 真实资产，不用 CSS 绘画或占位块代替；资产为 `2086 × 754`，在英雄区按 cover 裁切，人物和光束保持清晰。界面图标继续使用现有图标/文本组件，未新增手工 SVG 或伪造插画。

### 文案与内容

首页首屏围绕“今天，先完成最重要的一步”，并把当前赛事/任务直接呈现在主 CTA 上方；证据卡统一为“痛点证据 / 解决方案 / 已有进展”，与评委关心的价值、可行性、进展直接对应。工程页仍保留清晰的“上传 / 核对 / 入库”动作词，避免只剩装饰性英文。

## 交互与运行验收

- 管理员本地一键登录：成功进入 `#/admin`。
- 顶部导航：已通过首页、能力画像、赛事大厅、我的项目、智能顾问、数据维护等路由切换。
- 赛事检索：输入“蓝桥杯”后显示 `2 / 146 场赛事`，并出现蓝桥杯赛事卡。
- 项目时间线：点击时间线视图后显示“跨赛事执行时间线”，存在 13 个时间点，标签不重叠。
- 主 CTA 与智能顾问：首页“让智能顾问拆解”入口、顾问快捷问题和底部输入框均可见且可操作。
- 浏览器控制台：错误与警告日志为空（`[]`）。
- 静态检查：`node --check frontend/app.js` 通过；`env PYTHONPATH=src venv312/bin/python -m compileall -q src` 通过。

## 比较历史与修复记录

### 第一轮发现（P1）

- 智能顾问、数据维护部分仍显示白色工作区和浅色输入控件，与深色路演板产生明显视觉断裂。
- 能力画像选中卡片仍被旧的蓝白 `.test-type-card.active` 规则覆盖。

### 修复与复验

- 在 `frontend/styles.css` 末端加入高优先级主题隔离层：统一 agent workspace/titlebar/context/composer、admin stage/upload/form/radar ops，以及 profile active cards 的背景、边框、文字、焦点和状态色。
- 重新加载带版本号 `20260731.12` 的 CSS/JS 后，重新截取 [`design-qa-agent.png`](design-qa-agent.png)、[`design-qa-admin.png`](design-qa-admin.png) 和 [`design-qa-profile.png`](design-qa-profile.png)；三页均无浅色孤岛。
- 复验首页、赛事检索、时间线、移动端和控制台后，没有新的 P0/P1/P2 问题。移动端导航使用两行四列网格，避免横向裁切，同时保留全部一级入口。

### 本轮截图反馈迭代（P2）

- **发现：** 画像卡片仍有白色头像/能力标签；“科创作战地图”沿用旧白色路线卡；导航激活态带整块灰色背景，视觉重量过高。
- **修复：** 将头像、画像标签和技能标签改为暗色信息芯片；为 `.mission-map-shell`、`.mission-route`、节点、路径和图例补齐证据板 token；激活导航改为无背景的短金色下划线和焦点色，不再显示灰色矩形。
- **复验：** [`design-qa-profile-final.png`](design-qa-profile-final.png) 与 [`design-qa-map-final.png`](design-qa-map-final.png) 已在相同 `1487 × 1058` 视口重新捕获；画像卡片无白色孤岛，作战地图为暗色路线板，导航激活态为单一金线。
- **结果：** 未发现新的 P0/P1/P2 问题；现有交互和数据保持不变。

### 赛事卡片配色迭代（P3）

- **参考：** 用户提供的赛事大厅推荐卡片截图，重点吸收紫色类别、灰紫年份、薄荷绿报名状态、深绿推荐状态和暖金截止日期。
- **实现：** 在 `frontend/styles.css` 中为大厅推荐区和推荐页建立语义化标签色板；卡片边框、推荐区标题、侧栏截止日期和详情入口也同步到暖金 / 紫黑层级。
- **复验：** [`design-qa-palette-final.png`](design-qa-palette-final.png) 验证赛事大厅的 6 张专业推荐卡片；[`design-qa-recommend-palette.png`](design-qa-recommend-palette.png) 验证推荐页仍保持可读且不破坏原有交互。

### 画像保存栏迭代（P2）

- **发现：** 画像页的 `.profile-footer` 保留旧浅色背景，导致深色表单底部出现白色横条；同一区域的授权文字、状态消息与按钮颜色也不统一。表单最大宽度偏窄，造成右侧空白过多。
- **修复：** 保存栏改为深色卡面，授权信息改为暖灰可读文本；保存按钮使用暖金、删除按钮使用珊瑚红、成功/失败消息使用语义绿色/红色；表单最大宽度提升到 `1120px`。
- **复验：** [`design-qa-profile-footer.png`](design-qa-profile-footer.png) 在 `1487 × 1058` 视口确认：底部无白色区域、文字与操作按钮均清晰，表单宽度与页面节奏协调。

### 通知中心对齐迭代（P2）

- **发现：** “优先处理”列表使用内容自适应的三列布局，状态、正文和操作按钮在不同标题长度下无法共享稳定的列宽和基线。
- **修复：** 桌面端将通知项锁定为 `状态 84px / 内容自适应 / 操作 126px` 三列，操作按钮统一为右侧纵向操作栏；列表与辅助卡采用同一栅格节奏。窄屏改为内容与双按钮自然堆叠。
- **复验：** [`notification-after.png`](notification-after.png) 和 [`notification-mobile-after.png`](notification-mobile-after.png) 分别验证桌面、390px 窄屏；标题、说明、来源时间和操作按钮均已对齐，未出现裁切或重叠。

### 首页首屏迭代（P1 / P2）

- **发现：** 原首屏的抽象口号和当前任务并列，用户第一眼难以判断“现在该做什么”；英雄区、证据卡和关键数字卡叠放过重，空值统计呈现为生硬的 `0`。
- **修复：** 主标题改为“今天，先完成最重要的一步”，将当前赛事/任务置于高对比任务胶囊中；英雄区压缩高度、证据卡减少负边距并收紧内边距；关键数字改为“未来 7 天关键节点 / 待完成执行项 / 官方更新”，没有待办或临近截止时显示 `—` 与解释性提示，而非无意义的零值。
- **复验：** [`home-after.png`](home-after.png) 在 `1487 × 1058` 下确认首屏顺序为“画像 → 今日关键行动 → 行动证据 → 关键数字”，没有重叠或横向溢出；[`home-mobile-after.png`](home-mobile-after.png) 在 `390 × 844` 下确认英雄文案、任务胶囊、CTA 与行动信号均在单列内完整显示，导航 `scrollWidth = clientWidth = 362px`。浏览器错误与警告日志为 `[]`。

### 首页网格对齐与本地调试入口（P1）

- **发现：** 证据卡相对英雄区左右各内缩约 `34px`，并以负外边距压住英雄边线；统计卡与下方模块则使用全宽基线，导致连续浏览时边框和列线无法对齐。开发环境的一键管理员按钮因服务未设置本机开关而返回“本地调试入口未启用”。
- **修复：** 首页根容器统一为 `16px` 栅格节奏；英雄、证据、统计、项目与机会模块统一使用相同的全宽左右基线。证据卡取消负外边距，三列与统计卡采用同一组 `424 / 425 / 424px` 列宽和 `14px` 间距。当前本机服务以 `DEV_ADMIN_QUICK_LOGIN=1` 及仅本机令牌运行，生产环境仍保持默认关闭。
- **复验：** [`home-alignment-after.png`](home-alignment-after.png) 显示英雄、三张证据卡、三张统计卡和下方两栏均为 `left=93px / right=1394px` 的同一外边界；[`home-alignment-mobile-after.png`](home-alignment-mobile-after.png) 确认 `390px` 下英雄与证据区均为 `left=14px / right=376px`，页面无横向溢出。点击“本地调试：管理员一键登录”后成功跳转至 `#/admin`。

### 左侧栏拟态主题重构（P1）

- **设计方向：** 按已确认的拟态方案，将旧顶部导航改为固定左侧工作台；使用石墨黑、深灰蓝、暖金强调色和同源光影，凸起卡面表示可操作模块，内凹状态表示当前选中或数据槽位。
- **首页信息架构：** 首屏重新组织为“用户画像 → 今日最重要的一步 → 官方动态 → 资格 / 匹配 / 进度证据 → 关键数字 → 推荐与项目 → AI 快捷入口”，核心数据均来自现有接口，没有使用装饰性假指标。
- **全站一致性：** 画像、赛事大厅、匹配建议、行动路线、我的项目、机会提醒、智能顾问与数据维护共用同一侧栏和拟态表面语言；功能路由、权限控制和已有交互保持不变。
- **桌面复验：** [`neumorphic-home-final.png`](neumorphic-home-final.png)、[`neumorphic-hall-desktop.png`](neumorphic-hall-desktop.png)、[`neumorphic-projects-desktop.png`](neumorphic-projects-desktop.png) 与 [`neumorphic-agent-desktop.png`](neumorphic-agent-desktop.png) 覆盖首页、赛事、项目与顾问场景。首页在 `1440 × 900` 下 `scrollWidth = innerWidth = 1440px`，固定侧栏宽 `232px`，控制台日志为空。
- **移动复验：** [`neumorphic-home-mobile-final.png`](neumorphic-home-mobile-final.png) 在 `390 × 844` 下将侧栏转为紧凑九宫格导航，页面 `scrollWidth = innerWidth = 390px`，首页右边界为 `375px`，无横向裁切或文字遮挡。
- **权限复验：** 管理员入口继续由登录状态控制；本地一键登录可进入数据维护页，线上默认不开启调试接口。

### 赛事标签配色回收（P2）

- **发现：** 赛事卡片中的报名、推荐与专业匹配标签仍沿用上一版薄荷绿，和当前石墨黑 / 暖金 / 烟紫拟态主题产生跳色。
- **修复：** 报名与进度状态统一为低饱和暖金，正式推荐状态改为烟紫，专业匹配改为深沙金；保留类别紫色、年份灰紫和截止日期暖金的语义层级。
- **复验：** [`neumorphic-hall-palette-final.png`](neumorphic-hall-palette-final.png) 验证赛事大厅推荐区及普通赛事卡；正向标签计算样式均不再使用绿色，浏览器控制台日志为 `[]`。

## 实现清单

- [x] 统一深色路演证据板 token 与导航层级
- [x] 使用真实舞台背景资产并保持英雄文案可编辑
- [x] 首页证据叙事卡、行动信号和 CTA
- [x] 赛事大厅、详情、匹配建议、行动路线、通知中心、项目/时间线、组合和雷达主题统一
- [x] 智能顾问、数据维护、画像选中态旧浅色规则隔离
- [x] 桌面与移动端截图、关键交互、控制台和静态检查
- [x] 首页以“今日关键行动”为首屏重心，并完成桌面 / 移动端复验
- [x] 左侧栏拟态主题、首页重新编排与全站核心路由复验

## Follow-up Polish

- [P3] 如果后续需要更接近源图，可再补一轮数据可视化图表和头像/通知图标资产；当前版本优先保证真实数据、可读性和核心任务闭环。

final result: passed
